# claude

> Terminal CLI for agentic coding sessions.

<!-- tags: claude, cli, dev, resume -->

---

## resume last session max effort skip permissions
Resume the previous claude session at maximum reasoning effort, bypassing permission prompts.

```bash
claude --effort max --dangerously-skip-permissions --resume
```

<!-- meta: risk=high | phase=misc | tags=claude,resume,cli,skip-permissions -->
